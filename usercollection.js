
user = require('./user');
db = require('./database');

var UserCollection = function() {

	var _login = function(name, password) {
		// check name+password
		var bOk = true;
		if (bOk) {
			user = pin
		}
	}

	return  {
		login: function(name, password) {
			_login(name, password);
		}
	}
}();

module.exports = UserCollection;